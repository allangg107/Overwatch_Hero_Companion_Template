<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boss.testoverwatchherocompanion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/assets"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/assets_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TESTOverwatchHeroCompanion">
        <activity android:name="com.boss.testoverwatchherocompanion.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>